builds:
  - 
    ldflags:
      - -X github.com/nodyHub/fifi/main.version={{.Version}}
      - -X github.com/nodyHub/fifi/main.commit={{.Commit}}
      - -X github.com/nodyHub/fifi/main.date={{.CommitDate}}
      - -X github.com/nodyHub/fifi/main.builtBy=goreleaser-action
    env:
        - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm
      - arm64
    goarm:
      - 6
      - 7
archives:
  - replacements:
      darwin: Darwin
      linux: Linux
      windows: Windows
      386: i386
      amd64: x86_64
checksum:
  name_template: 'checksums.txt'
